{#
/**
 * @file logo-mark.svg.twig
 * Meadow Lane Park — nav logo mark.
 * Renders the community illustration icon (bridge, freighter, river).
 * Text has been removed from the oval for accessibility and clean rendering.
 * Alt text is provided on the parent <a> element in site-header.html.twig.
 */
#}
<img
  src="{{ base_path ~ directory }}/images/logo-icon.svg"
  alt=""
  aria-hidden="true"
  width="44"
  height="44"
  loading="eager"
/>
