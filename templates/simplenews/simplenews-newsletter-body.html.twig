{#
/**
 * @file simplenews-newsletter-body.html.twig
 * Renders the node body directly, suppressing the default h2 title.
 * Variables available: node, body, build, key, newsletter
 */
#}
{% if node.body.value %}
  {{ node.body.value|raw }}
{% elseif body %}
  {{ body|raw }}
{% endif %}
